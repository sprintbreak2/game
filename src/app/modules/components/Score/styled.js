import styled from 'styled-components';

export const StyledScore = styled.div`
    display: flex;
    align-items: center;

    p {
        color: #262533;
        font-size: 40px;
        font-weight: 700;
        text-transform: uppercase;
    }

    .score-value {
        margin-left: 1rem;
    }
`;